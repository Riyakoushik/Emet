{
  "name": "emet-landing-page",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist",
    "pages:login": "wrangler login"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "wrangler": "^4.73.0"
  }
}
